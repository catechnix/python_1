"""
series is columum
dataFrame is the whole table
index is the key, can use index to access item in the table

* Load the csv into a DataFrame
import pandas as pd
df= pd.read_csv('data.csv')
print(df.to_string())  //to_string to print the entire DataFrame

pd.options.display.max_rows = 9999 //to change the system max display rows to 9999

JSON=Python Dictionary

If JSON is not in a file, but in a Python dictionary, you can load it into
a DataFrame directly





"""
